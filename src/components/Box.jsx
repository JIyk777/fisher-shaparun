import styled from 'styled-components';
import {
  color,
  typography,
  layout,
  flexbox,
  background,
  space,
} from 'styled-system';

const Box = styled.div`
  ${color}
  ${typography}
  ${layout}
  ${flexbox}
  ${background}
  ${space}
`;

export default Box;
