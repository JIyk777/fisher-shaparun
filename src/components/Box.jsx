import styled from 'styled-components';
import {
  color,
  typography,
  layout,
  flexbox,
  background,
  space,
  border,
} from 'styled-system';

const Box = styled.div`
  ${color}
  ${typography}
  ${layout}
  ${flexbox}
  ${background}
  ${space}
  ${border}
`;

export default Box;
